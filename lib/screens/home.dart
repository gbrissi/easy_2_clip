import 'package:easy_2_clip/widgets/shared/shell.dart';
import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  const Home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Shell(
      child: Text("Easy2Clip"),
    );
  }
}
